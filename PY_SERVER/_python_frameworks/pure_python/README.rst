Pure Python Server Example
==========================

This is a basic Pthon HTTP server example. It does nothing more than
return an html page.

Requirements

    * Jinja2

Running the Server::

    python server.py

